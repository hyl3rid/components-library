import React from "react";
import Navbar from '../components/Navbar';

const Navbars = () => {
    return (
        <section className="navigation-bar__container">
            <Navbar position="center"/>
        </section>
    );
}

export default Navbars;
